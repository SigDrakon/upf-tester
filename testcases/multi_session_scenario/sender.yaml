testSteps:
  # 1. 建立会话 (UE IP: 10.250.0.1)
  - step: 1
    type: "session_establishment_request"
    action: "send"
    path: "establishment_sender.yaml"

  - step: 2
    type: "session_establishment_response"
    action: "recv"
  
  # 2. 等待接收端上线
  - step: 3
    type: "sleep"
    path: "3" 

  # 3. 发送数据 (Dst: 10.250.0.2)
  - step: 4
    type: "data_plane_test"
    action: "icmp"
    path: "icmp_sender_to_receiver.yaml"

  # # 4. 删除会话
  # - step: 5
  #   type: "session_deletion_request"
  #   action: "send"
  #   path: "deletion.yaml"

  # - step: 6
  #   type: "session_deletion_response"
  #   action: "recv"
